// Web stub of default_user_agent.dart: the browser owns the User-Agent
// header, so there is no client default to show.

/// Always null on web — the browser sets User-Agent itself.
String? defaultUserAgent() => null;
